<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Error <%= error.status || 500 %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light d-flex justify-content-center align-items-center" style="min-height: 100vh;">

    <div class="container text-center p-4 border rounded shadow bg-white">
        <h1 class="display-4 text-danger mb-3">Error <%= error.status || 500 %>
        </h1>
        <p class="lead">
            <%= message %>
        </p>

        <% if (error.stack && Object.keys(error).length> 0) { %>
            <div class="alert alert-secondary text-start mt-4" role="alert">
                <h5>Detalles técnicos:</h5>
                <pre class="mb-0"><%= error.stack %></pre>
            </div>
            <% } else { %>
                <p class="text-muted mt-4">Ha ocurrido un error inesperado.</p>
                <% } %>

                    <a href="/dashboard" class="btn btn-primary mt-3">Volver al inicio</a>
    </div>

</body>

</html>